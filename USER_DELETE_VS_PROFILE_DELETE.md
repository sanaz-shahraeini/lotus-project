# تفاوت بین حذف کاربر و حذف پروفایل

## خلاصه کوتاه
- **حذف کاربر (deleteUser)**: کاربر و تمام اطلاعات وابسته به آن کاملاً حذف می‌شود
- **حذف پروفایل (deleteProfile)**: فقط اطلاعات تکمیلی کاربر حذف می‌شود، اما حساب کاربری باقی می‌ماند

---

## جزئیات کامل

### 🔴 حذف کاربر (Delete User)
**آیکون**: 👤❌ (fa-user-slash)  
**رنگ**: قرمز (#FF5252)

#### چه چیزی حذف می‌شود:
1. **جدول Users**: تمام اطلاعات اصلی کاربر شامل:
   - نام کاربری (username)
   - نام و نام خانوادگی (name, lastname)
   - شماره داخلی (extension)
   - ایمیل (email)
   - گروه کاربری (group, groupname)
   - وضعیت فعال/غیرفعال (active)
   - دسترسی‌ها (usersextension)
   - رمز عبور (password)
   - تصویر پروفایل (picurl)

2. **جدول Permissions**: تمام مجوزهای کاربر شامل:
   - مجوز دیدن (can_view)
   - مجوز ایجاد (can_write)
   - مجوز ویرایش (can_modify)
   - مجوز حذف (can_delete)
   - مجوز گزارش خطا (errorsreport)
   - برچسب‌های دسترسی (exts_label)

3. **جدول Infos**: تمام اطلاعات تکمیلی شامل:
   - کد ملی (nationalcode)
   - تاریخ تولد (birthdate)
   - شماره تلفن (phonenumber, telephone)
   - آدرس (province, city, address)
   - اطلاعات شخصی (gender, maritalstatus, military)
   - اطلاعات تحصیلی (educationdegree, educationfield)
   - اطلاعات مالی (cardnumber, accountnumber, accountnumbershaba)

#### کد اجرایی:
```python
# Delete permissions
Permissions.objects.filter(user=user).delete()

# Delete info
Infos.objects.filter(user=user).delete()

# Delete user
user.delete()
```

#### نتیجه:
- کاربر کاملاً از سیستم حذف می‌شود
- امکان ورود به سیستم وجود ندارد
- تمام اطلاعات از دست می‌رود
- قابل بازگشت نیست

---

### 🟠 حذف پروفایل (Delete Profile)
**آیکون**: 🆔❌ (fa-id-card)  
**رنگ**: نارنجی (#FF7043)

#### چه چیزی حذف می‌شود:
فقط **جدول Infos** حذف می‌شود شامل:
- کد ملی (nationalcode)
- تاریخ تولد (birthdate)
- شماره تلفن (phonenumber, telephone)
- آدرس (province, city, address)
- اطلاعات شخصی (gender, maritalstatus, military)
- اطلاعات تحصیلی (educationdegree, educationfield)
- اطلاعات مالی (cardnumber, accountnumber, accountnumbershaba)

#### چه چیزی باقی می‌ماند:
1. **جدول Users**: تمام اطلاعات اصلی حفظ می‌شود
2. **جدول Permissions**: تمام مجوزها حفظ می‌شود

#### کد اجرایی:
```python
# Delete info only
Infos.objects.filter(user=user).delete()
```

#### نتیجه:
- کاربر همچنان می‌تواند وارد سیستم شود
- نام کاربری، رمز عبور و مجوزها تغییری نکرده
- فقط اطلاعات تکمیلی (پروفایل) پاک شده
- کاربر می‌تواند مجدداً اطلاعات پروفایل را تکمیل کند

---

## مقایسه سریع

| ویژگی | حذف کاربر | حذف پروفایل |
|--------|----------|-------------|
| **نام کاربری** | ❌ حذف | ✅ حفظ |
| **رمز عبور** | ❌ حذف | ✅ حفظ |
| **مجوزها** | ❌ حذف | ✅ حفظ |
| **شماره داخلی** | ❌ حذف | ✅ حفظ |
| **اطلاعات شخصی** | ❌ حذف | ❌ حذف |
| **امکان ورود** | ❌ خیر | ✅ بله |
| **قابل بازگشت** | ❌ خیر | ✅ بله (با تکمیل مجدد) |

---

## موارد استفاده

### کی از حذف کاربر استفاده کنیم:
- کاربر دیگر در سازمان نیست
- می‌خواهیم کاربر را کاملاً از سیستم حذف کنیم
- شماره داخلی را به کاربر دیگری اختصاص دهیم
- امنیت سیستم ایجاب می‌کند

### کی از حذف پروفایل استفاده کنیم:
- فقط می‌خواهیم اطلاعات شخصی را پاک کنیم
- کاربر همچنان باید دسترسی به سیستم داشته باشد
- می‌خواهیم حریم خصوصی را رعایت کنیم
- کاربر درخواست حذف اطلاعات شخصی کرده

---

## هشدارها

### ⚠️ حذف کاربر:
- **غیرقابل بازگشت**: پس از حذف، تمام اطلاعات از دست می‌رود
- **تأثیر بر گزارشات**: ممکن است گزارشات موجود تحت تأثیر قرار گیرد
- **شماره داخلی**: شماره داخلی آزاد می‌شود و قابل استفاده مجدد است

### ⚠️ حذف پروفایل:
- **حفظ دسترسی**: کاربر همچنان می‌تواند وارد سیستم شود
- **نمایش ناقص**: ممکن است برخی بخش‌های سیستم اطلاعات ناقص نمایش دهند
- **قابل تکمیل مجدد**: کاربر می‌تواند دوباره اطلاعات را وارد کند

---

## پیام‌های سیستم

### حذف کاربر موفق:
```
کاربر [نام کاربری] با موفقیت حذف شد.
```

### حذف پروفایل موفق:
```
پروفایل کاربر [نام کاربری] با موفقیت حذف شد.
```