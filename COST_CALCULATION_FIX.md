# اصلاح محاسبه هزینه تماس‌ها بر اساس استان

## 🔴 مشکل قبلی

قبل از این اصلاح، محاسبه هزینه تماس‌های داخل استانی و خارج استانی **به درستی انجام نمی‌شد**:

### مشکلات:
1. **استان انتخابی در تنظیمات استفاده نمی‌شد**
2. **منطق تشخیص provincial/outofprovincial اشتباه بود**:
   ```python
   # کد قبلی (اشتباه):
   "provincial" if int(number[0]) in range(1, 9)
   ```
   این کد فقط رقم اول شماره را چک می‌کرد و هیچ ارتباطی با استان انتخابی نداشت.

3. **تمام شماره‌های ثابت به عنوان "خارج استانی" محاسبه می‌شدند**

## ✅ راه‌حل اعمال شده

### تغییرات در `views.py` - تابع `callTypeDetector`:

#### 1. نقشه کدهای استان اضافه شد:
```python
PROVINCE_CODES = {
    '0': ['041'],      # آذربایجان شرقی - تبریز
    '1': ['044'],      # آذربایجان غربی - ارومیه
    '2': ['045'],      # اردبیل
    '3': ['031'],      # اصفهان
    '4': ['026'],      # البرز - کرج
    '5': ['084'],      # ایلام
    '6': ['077'],      # بوشهر
    '7': ['021'],      # تهران
    '8': ['038'],      # چهارمحال و بختیاری - شهرکرد
    '9': ['056'],      # خراسان جنوبی - بیرجند
    '10': ['051'],     # خراسان رضوی - مشهد
    '11': ['058'],     # خراسان شمالی - بجنورد
    '12': ['061'],     # خوزستان - اهواز
    '13': ['024'],     # زنجان
    '14': ['023'],     # سمنان
    '15': ['054'],     # سیستان و بلوچستان - زاهدان
    '16': ['071'],     # فارس - شیراز
    '17': ['028'],     # قزوین
    '18': ['025'],     # قم
    '19': ['087'],     # کردستان - سنندج
    '20': ['034'],     # کرمان
    '21': ['083'],     # کرمانشاه
    '22': ['074'],     # کهگیلویه و بویراحمد - یاسوج
    '23': ['017'],     # گلستان - گرگان
    '24': ['013'],     # گیلان - رشت
    '25': ['066'],     # لرستان - خرم آباد
    '26': ['011'],     # مازندران - ساری
    '27': ['086'],     # مرکزی - اراک
    '28': ['076'],     # هرمزگان - بندرعباس
    '29': ['081'],     # همدان
    '30': ['035'],     # یزد
}
```

#### 2. خواندن استان انتخابی از دیتابیس:
```python
try:
    contact_info = ContactInfo.objects.first()
    selected_province = contact_info.province if contact_info else None
except:
    selected_province = None
```

#### 3. منطق جدید تشخیص داخل/خارج استانی:
```python
if number[0] == "0" and len(number) >= 10:
    area_code = number[0:3]
    
    # اگر استان انتخاب شده باشد
    if selected_province and selected_province in PROVINCE_CODES:
        province_codes = PROVINCE_CODES[selected_province]
        if area_code in province_codes:
            return "provincial"  # داخل استانی
        else:
            # بررسی اینکه آیا کد استان معتبر است
            all_area_codes = [code for codes in PROVINCE_CODES.values() for code in codes]
            if area_code in all_area_codes:
                return "outofprovincial"  # خارج استانی
```

## 📊 نحوه عملکرد

### مثال 1: تماس داخل استانی (مشهد)
- **استان انتخابی**: خراسان رضوی (کد: `10`)
- **شماره تماس**: `05136057970`
- **کد استان شماره**: `051` (مشهد)
- **نتیجه**: `provincial` → هزینه: **45 ریال/دقیقه**

### مثال 2: تماس خارج استانی (تهران)
- **استان انتخابی**: خراسان رضوی (کد: `10`)
- **شماره تماس**: `02188776655`
- **کد استان شماره**: `021` (تهران)
- **نتیجه**: `outofprovincial` → هزینه: **330 ریال/دقیقه**

### مثال 3: تماس موبایل (همراه اول)
- **شماره تماس**: `09197475417`
- **پیش‌شماره**: `0919`
- **نتیجه**: `hamrahaval` → هزینه: **625 ریال/دقیقه**

## 🔄 فرآیند محاسبه هزینه

1. **ورود شماره تماس** → `callTypeDetector(number)`
2. **نرمال‌سازی شماره** (حذف +98، 0098، و...)
3. **بررسی اپراتورهای موبایل** (ایرانسل، همراه اول، رایتل)
4. **بررسی بین‌المللی**
5. **بررسی داخل/خارج استانی** (بر اساس استان انتخابی)
6. **دریافت قیمت** از جدول `Costs` → `getPrice(calltype)`
7. **محاسبه هزینه** → `calculatePrice(duration, price)`
   - فرمول: `ceil(seconds / 60) × price_per_minute`

## ⚙️ تنظیمات مورد نیاز

### در صفحه تنظیمات سیستم (`/settings`):

1. **انتخاب استان**:
   - ساپورتر باید استان خود را از لیست انتخاب کند
   - این اطلاعات در جدول `ContactInfo` ذخیره می‌شود

2. **تعیین هزینه‌های تماس**:
   - **داخل استانی**: 45 ریال/دقیقه
   - **خارج استانی**: 330 ریال/دقیقه
   - **ایرانسل**: 65 ریال/دقیقه
   - **همراه اول**: 65 ریال/دقیقه
   - **رایتل**: 65 ریال/دقیقه
   - **بین‌المللی**: 3400 ریال/دقیقه

## 📝 نکات مهم

1. **اگر استان انتخاب نشود**: همه تماس‌های ثابت به عنوان "خارج استانی" محاسبه می‌شوند
2. **تماس‌های موبایل**: بدون توجه به استان، بر اساس اپراتور محاسبه می‌شوند
3. **گرد کردن زمان**: زمان تماس همیشه به سقف دقیقه گرد می‌شود (`math.ceil`)
4. **شماره‌های داخلی** (کمتر از 5 رقم): هزینه ندارند

## 🧪 تست

برای تست صحت عملکرد:

1. در صفحه تنظیمات، استان "خراسان رضوی" را انتخاب کنید
2. یک تماس با شماره `05136057970` ثبت کنید → باید "داخل استانی" باشد
3. یک تماس با شماره `02188776655` ثبت کنید → باید "خارج استانی" باشد
4. یک تماس با شماره `09197475417` ثبت کنید → باید "همراه اول" باشد
5. در داشبورد، هزینه‌ها را بررسی کنید

## 📌 فایل‌های تغییر یافته

- ✅ `Alvand/views.py` - تابع `callTypeDetector` (خطوط 289-388)

## 🎯 نتیجه

با این اصلاح:
- ✅ محاسبه هزینه بر اساس استان انتخابی انجام می‌شود
- ✅ تماس‌های داخل استانی با قیمت 45 ریال محاسبه می‌شوند
- ✅ تماس‌های خارج استانی با قیمت 330 ریال محاسبه می‌شوند
- ✅ منطق تشخیص نوع تماس کاملاً اصلاح شد
- ✅ کد تمیز و قابل نگهداری است
