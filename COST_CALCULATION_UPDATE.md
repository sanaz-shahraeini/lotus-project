# به‌روزرسانی: محاسبه هزینه بر اساس ثانیه

## 🔄 تغییر اعمال شده

### **قبل (محاسبه بر اساس دقیقه با گرد کردن):**
```python
def calculatePrice(duration: str, price: int) -> int:
    hour, minute, second = map(int, duration.split(":"))
    toSeconds = (hour * 3600) + (minute * 60) + second
    toMinutes = math.ceil(toSeconds / 60)  # گرد کردن به سقف دقیقه
    return toMinutes * price
```

**مشکل:** حتی 1 ثانیه تماس، به عنوان 1 دقیقه کامل محاسبه می‌شد!

### **بعد (محاسبه دقیق بر اساس ثانیه):**
```python
def calculatePrice(duration: str, price: int) -> float:
    hour, minute, second = map(int, duration.split(":"))
    toSeconds = (hour * 3600) + (minute * 60) + second
    # محاسبه بر اساس ثانیه: (ثانیه / 60) × قیمت هر دقیقه
    return (toSeconds / 60) * price
```

**فرمول جدید:**
```
هزینه = (کل_ثانیه_تماس ÷ 60) × قیمت_هر_دقیقه
```

## 📊 مقایسه محاسبات

### مثال 1: تماس 17 ثانیه‌ای با همراه اول
| روش | محاسبه | نتیجه |
|-----|--------|-------|
| **قبل (گرد به دقیقه)** | `ceil(17/60) × 625 = 1 × 625` | **625 ریال** |
| **بعد (بر اساس ثانیه)** | `(17/60) × 625 = 0.283 × 625` | **177.08 ریال** |

### مثال 2: تماس 3 دقیقه و 8 ثانیه با همراه اول
| روش | محاسبه | نتیجه |
|-----|--------|-------|
| **قبل (گرد به دقیقه)** | `ceil(188/60) × 625 = 4 × 625` | **2,500 ریال** |
| **بعد (بر اساس ثانیه)** | `(188/60) × 625 = 3.133 × 625` | **1,958.33 ریال** |

### مثال 3: تماس 26 ثانیه‌ای خارج استانی
| روش | محاسبه | نتیجه |
|-----|--------|-------|
| **قبل (گرد به دقیقه)** | `ceil(26/60) × 330 = 1 × 330` | **330 ریال** |
| **بعد (بر اساس ثانیه)** | `(26/60) × 330 = 0.433 × 330` | **143 ریال** |

## 🎯 مزایای روش جدید

1. ✅ **عادلانه‌تر**: کاربر فقط برای ثانیه‌های واقعی تماس پرداخت می‌کند
2. ✅ **دقیق‌تر**: محاسبه با اعشار انجام می‌شود
3. ✅ **شفاف‌تر**: هزینه متناسب با زمان واقعی است
4. ✅ **استاندارد**: اکثر اپراتورها به همین روش محاسبه می‌کنند

## 📝 نکات مهم

### 1. نمایش اعداد اعشاری
در template tag باید فرمت نمایش را تنظیم کنید:

```python
# در dashboardTags.py
return f"{calculate:,.2f}"  # نمایش با 2 رقم اعشار
```

### 2. محاسبه مجموع هزینه
در JavaScript داشبورد، مجموع هزینه‌ها به درستی جمع می‌شود:

```javascript
function calculateTotalCosts() {
    let total = 0;
    $('#tbody tr:visible').each(function() {
        const costText = $(this).find('.cost span.font-medium').text().replace(/,/g, '');
        if ($.isNumeric(costText)) { 
            total += parseFloat(costText); // استفاده از parseFloat برای اعداد اعشاری
        }
    });
    $('#totalCosts').text(total.toLocaleString('fa-IR', { 
        minimumFractionDigits: 2, 
        maximumFractionDigits: 2 
    }));
}
```

## 🧪 تست با داده‌های تصویر

بر اساس تصویر ارسالی:

| ردیف | شماره | مدت | نوع | قیمت قبل | قیمت جدید |
|------|-------|-----|-----|----------|-----------|
| 32 | +989122723750 | 00:03:08 (188s) | همراه اول | 2,500 | **1,958.33** |
| 37 | +989122723750 | 00:00:17 (17s) | همراه اول | 625 | **177.08** |
| 40 | +983136120001 | 00:00:26 (26s) | خارج استانی | 330 | **143** |

## ⚠️ توجه

اگر می‌خواهید **حداقل 1 دقیقه** محاسبه شود (مثل برخی اپراتورها):

```python
def calculatePrice(duration: str, price: int) -> float:
    hour, minute, second = map(int, duration.split(":"))
    toSeconds = (hour * 3600) + (minute * 60) + second
    
    # حداقل 1 دقیقه محاسبه شود
    if toSeconds > 0 and toSeconds < 60:
        toSeconds = 60
    
    return (toSeconds / 60) * price
```

## 📌 فایل‌های تغییر یافته

- ✅ `Alvand/views.py` - تابع `calculatePrice` (خط 150-155)

## 🎉 نتیجه

با این تغییر:
- ✅ هزینه به صورت دقیق بر اساس ثانیه محاسبه می‌شود
- ✅ کاربران فقط برای زمان واقعی تماس هزینه می‌پردازند
- ✅ محاسبات عادلانه‌تر و شفاف‌تر است
